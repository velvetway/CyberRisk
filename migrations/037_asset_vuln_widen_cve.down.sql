-- Откат: длинные CVE-списки могут не влезть, но в данных без длинных
-- инвентарей этого не случится. Указываем VARCHAR(255) как разумный
-- компромисс — оригинальные 64 байта тоже мало для такого поля.
ALTER TABLE asset_vulnerabilities
    ALTER COLUMN cve TYPE VARCHAR(255);
